<%= form_for(pago, remote: true, html: { id: "pago_id"}) do |f| %>
  <% if pago.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pago.errors.count, "error") %> prohibited this pago from being saved:</h2>

      <ul>
      <% pago.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="panel panel-default">
    <div class="panel-body">
      <div class="row">
        <div class="col-md-6">
          <div class="form-group">
            <%= f.label :factura_id, "Factura *",class: "control-label" %>
            <%= f.number_field :factura_id, required: true, minlength: 1, maxlength: 8, class: "form-control uppercase" %>
          </div>
        </div>
        <div class="col-md-6">
          <div class="form-group">
            <%= f.label :fechapago, "Fecha de pago *",class: "control-label" %>
            <%= f.date_field :fechapago, required: true, class: "form-control" %>
          </div>
        </div>
      </div>

      <div class="actions">
        <%= f.submit class: 'btn btn-default'%>
        <%= link_to "Cancelar", "", type: "reset", class: "btn btn-default" %>
        <%= link_to "Atrás", pagos_path, class: "btn btn-default" %>
      </div>
      
    </div>
  </div>
<% end %>